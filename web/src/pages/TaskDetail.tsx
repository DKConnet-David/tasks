// Tech-side task detail page: shows Splynx task fields, captures photos via
// the device camera (input capture="environment"), accepts a free-text comment,
// and submits to the backend pipeline. Implementation comes in Phase 3.

export function TaskDetail() {
  return (
    <div className="container">
      <em className="muted">TaskDetail — implementation pending (Phase 3).</em>
    </div>
  );
}
