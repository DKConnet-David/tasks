// Admin list of submissions with filters (status, tech, date, search) and a
// rating column (admin score if set, AI score otherwise). Implemented in Phase 8.

export function SubmissionsList() {
  return (
    <div className="panel">
      <em className="muted">SubmissionsList — implementation pending (Phase 8).</em>
    </div>
  );
}
