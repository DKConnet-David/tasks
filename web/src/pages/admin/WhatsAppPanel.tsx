// Admin WhatsApp onboarding/status panel: shows the QR code (if waiting for
// scan), the connection state, and the configured group's name + JID.
// Implemented in Phase 6.

export function WhatsAppPanel() {
  return (
    <div className="panel">
      <em className="muted">WhatsApp panel — implementation pending (Phase 6).</em>
    </div>
  );
}
