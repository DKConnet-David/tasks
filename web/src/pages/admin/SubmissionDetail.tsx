// Admin detail page: photos, editable AI summary (writes back to Splynx),
// editable raw tech comment (local only), PDF preview, action buttons
// (resend WhatsApp, re-attach Splynx, regenerate, mark resolved), and the
// admin-only rating block at the bottom. Implemented in Phase 9 + Phase 10.

export function SubmissionDetail() {
  return (
    <div className="panel">
      <em className="muted">SubmissionDetail — implementation pending (Phase 9–10).</em>
    </div>
  );
}
