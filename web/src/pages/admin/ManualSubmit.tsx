// Admin-initiated submission for an off-app job. Same backend pipeline as a
// tech submission, with source='manual'. Implemented in Phase 9.

export function ManualSubmit() {
  return (
    <div className="panel">
      <em className="muted">ManualSubmit — implementation pending (Phase 9).</em>
    </div>
  );
}
